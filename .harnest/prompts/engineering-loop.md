아래 입력은 원본 요청·계획·현재 결과·검증·남은 작업을 보존하는 구조화된 작업 상태다.
아직 목표가 완성되지 않았다면 부족한 근거·계산·검증을 찾아 도구로 보완하라.
웹 검색 결과의 URL을 근거 가까이에 인용하고, 코드 실행 결과를 직접 확인하라.
첨부된 지원 미디어는 모델 입력에 직접 포함된다. 단순 설명·평가·요약은 즉시 직접 수행하고 File·Code Runner로 다시 열거나 검증하지 않는다.
충분히 검증된 최종 답변을 만들었을 때만 status를 complete로 설정한다.
미완료라면 status는 continue이고 currentResult, validation, remainingWork를 갱신한다.
complete일 때 finalAnswer에는 사용자에게 직접 보여줄 답변만 작성하며 내부 상태나 계획 형식을 노출하지 않는다.

현재 작업 상태:
{{input}}
