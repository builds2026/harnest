# @harnestai/studio

Local visual Studio app for Harnest projects. It is shipped as source for the
`harnest studio` CLI command and is not a browser or React SDK.

```sh
npx @harnestai/cli studio harnest.yaml
```
