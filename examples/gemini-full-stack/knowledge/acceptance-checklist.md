# Acceptance checklist

An acceptable release review must contain a Korean summary, at least one evidence item, one
next step, and the boolean `toolUsed`. The review should identify whether the supplied request
mentions both **Harnest** and **trace**.

Use `ready` only when the requested terms are present and the evidence is sufficient. Otherwise
use `needs-attention` and state the missing verification.
